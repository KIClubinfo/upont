version: "2.3"

services:
  upont-front:
    build:
      context: front

  upont-back:
    build:
      context: back
      args:
        CIRCLE_SHA1: ${CIRCLE_SHA1}
        CIRCLE_TAG: ${CIRCLE_TAG}

  ingress:
    build:
      context: docker/ingress
